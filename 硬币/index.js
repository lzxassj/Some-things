/**
 * @param {number} n
 * @return {number}
 */
var waysToChange = function (n) {
  const a = parseInt(n / 25);
  const b = parseInt(n / 10);
  const c = parseInt(n / 5);
  const d = n;


  return a;
};

const x = 10;
console.log(waysToChange(x));

getCell(0,0)
